// Stub for react-devtools-core (only loaded by ink when DEV=true).
// Used as a Bun bundle alias to avoid pulling in the real package.
export default {
	initialize: () => {},
	connectToDevTools: () => {},
};
